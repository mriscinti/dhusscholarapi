# Go to your project directory (adjust path if needed)
cd path/to/your-project-folder

# Initialize Git
git init

# Add all files
git add .

# Commit with a message
git commit -m "Initial commit"

# Set branch to main
git branch -M main

# Add your GitHub repo as the remote
git remote add origin https://github.com/mriscinti/dhusscholarapi2.git

# Push to GitHub
git push -u origin main
